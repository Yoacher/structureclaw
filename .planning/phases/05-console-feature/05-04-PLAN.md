---
phase: 05-console-feature
plan: 04
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - frontend/src/components/console/artifacts-list.tsx
  - frontend/src/components/console/debug-output.tsx
  - frontend/src/components/console/index.ts
  - frontend/tests/components/console/artifacts-list.test.tsx
  - frontend/tests/components/console/debug-output.test.tsx
autonomous: true
requirements:
  - CONS-11
  - CONS-14
user_setup: []
must_haves:
  truths:
    - "User can view artifacts list with format and path"
    - "User can view raw JSON output"
    - "User can view stream frames captured during SSE"
    - "Debug output uses monospace font for readability"
  artifacts:
    - path: "frontend/src/components/console/artifacts-list.tsx"
      provides: "Artifacts list display"
      exports: ["ArtifactsList"]
    - path: "frontend/src/components/console/debug-output.tsx"
      provides: "Debug output panel with raw JSON and stream frames"
      exports: ["DebugOutput"]
  key_links:
    - from: "frontend/src/components/console/artifacts-list.tsx"
      to: "frontend/src/lib/stores/slices/console.ts"
      via: "result.artifacts"
      pattern: "state\\.result\\.artifacts"
    - from: "frontend/src/components/console/debug-output.tsx"
      to: "frontend/src/lib/stores/slices/console.ts"
      via: "rawResponse, streamFrames, error"
      pattern: "state\\.(rawResponse|streamFrames|error)"
---

<objective>
Build artifacts list and debug output components for viewing execution artifacts and debugging.

Purpose: Provide visibility into execution outputs and raw data for debugging.
Output: ArtifactsList, DebugOutput components
</objective>

<execution_context>
@/home/guyi/.claude/get-shit-done/workflows/execute-plan.md
@/home/guyi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-console-feature/05-RESEARCH.md
@.planning/phases/05-console-feature/05-VALIDATION.md

## Artifacts Type

```typescript
interface Artifact {
  format: string
  path: string
}

// In AgentResult
artifacts?: Array<{ format: string; path: string }>
```

## Console Execution State (from 05-01)

```typescript
interface ConsoleExecutionState {
  rawResponse: string      // Raw JSON from API response
  streamFrames: string[]   // Captured SSE frames
  error: string | null     // Error message if any
}
```

## Existing UI Components

From frontend/src/components/ui/card.tsx:
- Card, CardHeader, CardTitle, CardContent, CardDescription

From frontend/src/components/ui/separator.tsx:
- Separator component
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ArtifactsList component</name>
  <files>
    frontend/src/components/console/artifacts-list.tsx
    frontend/tests/components/console/artifacts-list.test.tsx
  </files>
  <behavior>
    - ArtifactsList renders a list of artifacts
    - Each artifact shows format and path
    - Hidden when no artifacts
    - Uses bullet list or table layout
  </behavior>
  <action>
    Create the ArtifactsList component for displaying execution artifacts.

    1. Create `frontend/src/components/console/artifacts-list.tsx`:
       - Props: artifacts (Array<{ format: string; path: string }> | undefined)
       - Return null if no artifacts or empty array
       - Render section title "Artifacts"
       - Render bullet list with format: path for each artifact
       - Use Card or simple div container
       - Follow existing AgentConsole artifacts display pattern

    2. Create `frontend/tests/components/console/artifacts-list.test.tsx`:
       - Test: returns null when no artifacts
       - Test: returns null when empty array
       - Test: renders all artifacts when provided
       - Test: displays format and path for each artifact
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run tests/components/console/artifacts-list.test.tsx</automated>
  </verify>
  <done>ArtifactsList component displays execution artifacts</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create DebugOutput component</name>
  <files>
    frontend/src/components/console/debug-output.tsx
    frontend/tests/components/console/debug-output.test.tsx
  </files>
  <behavior>
    - DebugOutput renders error message at top if present
    - DebugOutput renders "Raw JSON" section with rawResponse in pre/code block
    - DebugOutput renders "Stream Frames" section with streamFrames joined by newlines
    - Uses monospace font (font-mono) for code blocks
    - Shows "None" placeholder when no data
  </behavior>
  <action>
    Create the DebugOutput component for viewing raw API response and stream frames.

    1. Create `frontend/src/components/console/debug-output.tsx`:
       - Use useStore to access rawResponse, streamFrames, error
       - Use Card component for container with CardHeader and CardContent
       - Render error message at top (if present) with error styling
       - Render "Raw JSON" section with pre/code block
       - Render "Stream Frames" section with pre/code block
       - Use font-mono Tailwind class for monospace styling
       - Show placeholder text ("None" / "No frames") when empty
       - Follow existing AgentConsole debug output pattern

    2. Create `frontend/tests/components/console/debug-output.test.tsx`:
       - Test: renders without crashing
       - Test: shows error message when present
       - Test: shows "None" when no raw response
       - Test: shows raw JSON when present
       - Test: shows "No frames" when no stream frames
       - Test: shows stream frames when present
       - Test: uses monospace font for code blocks
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run tests/components/console/debug-output.test.tsx</automated>
  </verify>
  <done>DebugOutput component displays raw JSON and stream frames</done>
</task>

<task type="auto">
  <name>Task 3: Update console barrel export</name>
  <files>frontend/src/components/console/index.ts</files>
  <action>
    Update barrel export to include artifacts and debug components.

    Update `frontend/src/components/console/index.ts`:
    ```typescript
    export { EndpointSelector } from './endpoint-selector'
    export { MessageInput } from './message-input'
    export { ModelJsonPanel } from './model-json-panel'
    export { ConfigPanel } from './config-panel'
    export { ExecuteButton } from './execute-button'
    export { ResultDisplay, StatusHeader, MetricsGrid } from './result-display'
    export { Timeline } from './timeline'
    export { ReportSummary } from './report-summary'
    export { ArtifactsList } from './artifacts-list'
    export { DebugOutput } from './debug-output'
    ```
  </action>
  <verify>
    <automated>grep -q "ArtifactsList" frontend/src/components/console/index.ts && grep -q "DebugOutput" frontend/src/components/console/index.ts</automated>
  </verify>
  <done>Barrel export updated with ArtifactsList and DebugOutput</done>
</task>

</tasks>

<verification>
- ArtifactsList component with tests
- DebugOutput component with tests
- All tests pass: `cd frontend && npm test -- --run`
</verification>

<success_criteria>
- CONS-11: User can view artifacts list with format and path
- CONS-14: User can view debug output (raw JSON, stream frames)
</success_criteria>

<output>
After completion, create `.planning/phases/05-console-feature/05-04-SUMMARY.md`
</output>
