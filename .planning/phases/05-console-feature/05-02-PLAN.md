---
phase: 05-console-feature
plan: 02
type: execute
wave: 2
depends_on: ["05-00", "05-01"]
files_modified:
  - frontend/src/components/console/config-panel.tsx
  - frontend/src/components/console/execute-button.tsx
  - frontend/src/components/console/index.ts
  - frontend/src/hooks/use-console-execution.ts
  - frontend/tests/components/console/config-panel.test.tsx
  - frontend/tests/components/console/execute-button.test.tsx
  - frontend/tests/hooks/use-console-execution.test.ts
  - frontend/tests/integration/console-execution.test.tsx
autonomous: true
requirements:
  - CONS-05
  - CONS-06
  - CONS-07
  - CONS-12
user_setup: []
must_haves:
  truths:
    - "User can configure analysis options (analysisType, reportFormat, reportOutput)"
    - "User can toggle checkboxes for includeModel, autoAnalyze, autoCodeCheck, includeReport"
    - "User can execute requests with execute button"
    - "Execute button shows loading state during execution"
    - "useConsoleExecution hook handles sync and stream execution"
  artifacts:
    - path: "frontend/src/components/console/config-panel.tsx"
      provides: "Configuration options and checkboxes"
      exports: ["ConfigPanel"]
    - path: "frontend/src/components/console/execute-button.tsx"
      provides: "Execute button with loading state"
      exports: ["ExecuteButton"]
    - path: "frontend/src/hooks/use-console-execution.ts"
      provides: "Execution hook for sync and SSE requests"
      exports: ["useConsoleExecution"]
  key_links:
    - from: "frontend/src/components/console/execute-button.tsx"
      to: "frontend/src/hooks/use-console-execution.ts"
      via: "useConsoleExecution hook"
      pattern: "useConsoleExecution"
    - from: "frontend/src/hooks/use-console-execution.ts"
      to: "frontend/src/lib/stores/slices/console.ts"
      via: "useStore selector"
      pattern: "useStore"
    - from: "frontend/src/hooks/use-console-execution.ts"
      to: "frontend/src/lib/api/client.ts"
      via: "api.post"
      pattern: "api\\.post"
---

<objective>
Build configuration panel with options/checkboxes and execute button with execution hook.

Purpose: Provide the control layer for configuring and triggering console requests.
Output: ConfigPanel, ExecuteButton components + useConsoleExecution hook
</objective>

<execution_context>
@/home/guyi/.claude/get-shit-done/workflows/execute-plan.md
@/home/guyi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-console-feature/05-RESEARCH.md
@.planning/phases/05-console-feature/05-VALIDATION.md

## Extended Console Slice (from 05-01)

From frontend/src/lib/stores/slices/console.ts:
```typescript
// Configuration state
analysisType: 'static' | 'dynamic' | 'seismic' | 'nonlinear'
reportFormat: 'json' | 'markdown' | 'both'
reportOutput: 'inline' | 'file'
autoAnalyze: boolean
autoCodeCheck: boolean
includeReport: boolean

// Execution state
loading: boolean
isStreaming: boolean
connectionState: 'idle' | 'connecting' | 'receiving' | 'complete' | 'error'
result: AgentResult | null
error: string | null

// Actions (from 05-01)
setAnalysisType, setReportFormat, setReportOutput
setAutoAnalyze, setAutoCodeCheck, setIncludeReport
setLoading, setConnectionState, setResult, setError
```

## API Client Pattern

From frontend/src/lib/api/client.ts:
```typescript
export const api = {
  post: <T>(endpoint: string, body?: unknown, options?: Omit<RequestOptions, 'method' | 'body'>) =>
    apiClient<T>(endpoint, { ...options, method: 'POST', body }),
}
```

## Existing SSE Hook Pattern

From frontend/src/hooks/use-sse.ts:
```typescript
export function useSSE({
  url,
  enabled = true,
  onMessage,
  onError,
}: UseSSEOptions): UseSSEReturn
```

## API Endpoints

From existing AgentConsole:
- agent-run: POST /api/v1/agent/run
- chat-message: POST /api/v1/chat/message
- chat-execute: POST /api/v1/chat/execute
- stream: POST /api/v1/chat/stream (SSE)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ConfigPanel component</name>
  <files>
    frontend/src/components/console/config-panel.tsx
    frontend/tests/components/console/config-panel.test.tsx
  </files>
  <behavior>
    - ConfigPanel renders analysisType, reportFormat, reportOutput selects in a 3-column grid
    - ConfigPanel renders checkbox row with: includeModel, autoAnalyze, autoCodeCheck, includeReport
    - Selecting an option updates the Zustand store
    - Toggling a checkbox updates the Zustand store
  </behavior>
  <action>
    Create the ConfigPanel component using Select and Checkbox, connected to Zustand store.

    1. Create `frontend/src/components/console/config-panel.tsx`:
       - Use Select components from @/components/ui/select
       - Use Checkbox from @/components/ui/checkbox
       - Use useStore to access configuration state and actions
       - Render 3-column grid for analysisType, reportFormat, reportOutput selects
       - Render checkbox row with 4 checkboxes and labels
       - Apply cn() for consistent styling

    2. Create `frontend/tests/components/console/config-panel.test.tsx`:
       - Test: renders all three selects
       - Test: analysisType select has correct options
       - Test: reportFormat select has correct options
       - Test: reportOutput select has correct options
       - Test: renders all four checkboxes
       - Test: selecting an option updates store
       - Test: toggling checkbox updates store
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run tests/components/console/config-panel.test.tsx</automated>
  </verify>
  <done>ConfigPanel renders configuration options and checkboxes connected to store</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create useConsoleExecution hook</name>
  <files>
    frontend/src/hooks/use-console-execution.ts
    frontend/tests/hooks/use-console-execution.test.ts
  </files>
  <behavior>
    - executeSync sends POST request with correct payload based on endpoint
    - executeSync validates model JSON before sending (returns error if invalid)
    - executeSync updates loading state and connectionState
    - executeSync sets result or error on completion
    - executeStream handles SSE connection with reader-based streaming
    - executeStream parses stream frames and updates streamFrames array
    - executeStream updates connectionState: connecting -> receiving -> complete
  </behavior>
  <action>
    Create the useConsoleExecution hook that encapsulates sync and stream execution logic.

    1. Create `frontend/src/hooks/use-console-execution.ts`:
       - Use useStore to access form state and execution actions
       - Use api.post for sync requests
       - Use fetch + reader for stream requests (follow existing AgentConsole pattern)
       - Implement executeSync():
         - Validate model JSON if includeModel is true
         - Build request payload based on endpoint type
         - Set loading=true, connectionState='connecting', clear previous results
         - POST to correct endpoint
         - On success: set result, connectionState='complete'
         - On error: set error, connectionState='error'
         - Finally: set loading=false
       - Implement executeStream():
         - Similar validation and state setup
         - Use fetch with POST to /api/v1/chat/stream
         - Use reader.read() loop to process stream
         - Parse SSE frames (data: prefix)
         - Update streamFrames array
         - Parse result frame and update result state

    2. Update `frontend/tests/hooks/use-console-execution.test.ts`:
       - Replace it.todo() with actual tests
       - Use MSW or mock fetch for testing
       - Test: executeSync sends correct payload
       - Test: executeSync validates model JSON
       - Test: executeSync handles API errors
       - Test: executeStream handles SSE connection
       - Test: executeStream parses stream frames

    Reference the existing AgentConsole runRequest() and runStream() implementations for the exact payload structure and streaming logic.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run tests/hooks/use-console-execution.test.ts</automated>
  </verify>
  <done>useConsoleExecution hook handles sync and stream execution with proper state management</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create ExecuteButton component</name>
  <files>
    frontend/src/components/console/execute-button.tsx
    frontend/tests/components/console/execute-button.test.tsx
  </files>
  <behavior>
    - ExecuteButton renders two buttons: "Send Request" and "Stream (SSE)"
    - Buttons are disabled when loading is true
    - "Send Request" button calls executeSync
    - "Stream (SSE)" button calls executeStream
    - Loading state shows spinner or disabled styling
  </behavior>
  <action>
    Create the ExecuteButton component using Button, connected to useConsoleExecution hook.

    1. Create `frontend/src/components/console/execute-button.tsx`:
       - Use Button from @/components/ui/button
       - Use useStore to access loading state
       - Use useConsoleExecution hook
       - Render two buttons in a row
       - "Send Request" (primary variant) calls executeSync
       - "Stream (SSE)" (secondary/outline variant) calls executeStream
       - Both disabled when loading is true
       - Optional: Add Loader2 icon for loading state

    2. Create `frontend/tests/components/console/execute-button.test.tsx`:
       - Test: renders both buttons
       - Test: buttons are disabled when loading
       - Test: clicking "Send Request" calls executeSync
       - Test: clicking "Stream (SSE)" calls executeStream
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run tests/components/console/execute-button.test.tsx</automated>
  </verify>
  <done>ExecuteButton renders with loading state and triggers execution</done>
</task>

<task type="auto">
  <name>Task 4: Update console barrel export</name>
  <files>frontend/src/components/console/index.ts</files>
  <action>
    Update barrel export to include new components.

    Update `frontend/src/components/console/index.ts`:
    ```typescript
    export { EndpointSelector } from './endpoint-selector'
    export { MessageInput } from './message-input'
    export { ModelJsonPanel } from './model-json-panel'
    export { ConfigPanel } from './config-panel'
    export { ExecuteButton } from './execute-button'
    ```
  </action>
  <verify>
    <automated>grep -q "ConfigPanel" frontend/src/components/console/index.ts && grep -q "ExecuteButton" frontend/src/components/console/index.ts</automated>
  </verify>
  <done>Barrel export updated with ConfigPanel and ExecuteButton</done>
</task>

<task type="auto">
  <name>Task 5: Create integration test for console execution</name>
  <files>frontend/tests/integration/console-execution.test.tsx</files>
  <action>
    Create an integration test that verifies the full execution flow.

    1. Create `frontend/tests/integration/console-execution.test.tsx`:
       - Test: filling form and clicking execute updates store with result
       - Test: error handling shows error in store
       - Use mock fetch or MSW for API mocking
       - Wrap in AppStoreProvider
       - Test the complete flow: input -> click -> state update

    This test verifies the integration between:
    - EndpointSelector (form state)
    - MessageInput (form state)
    - ConfigPanel (form state)
    - ExecuteButton (triggers execution)
    - useConsoleExecution (API calls)
    - Console store (state updates)
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run tests/integration/console-execution.test.tsx</automated>
  </verify>
  <done>Integration test verifies full execution flow</done>
</task>

</tasks>

<verification>
- ConfigPanel component with tests
- useConsoleExecution hook with tests
- ExecuteButton component with tests
- Integration test for execution flow
- All tests pass: `cd frontend && npm test -- --run`
</verification>

<success_criteria>
- CONS-05: User can configure analysis options (analysisType, reportFormat, reportOutput)
- CONS-06: User can toggle checkboxes (includeModel, autoAnalyze, autoCodeCheck, includeReport)
- CONS-07: Execute button triggers sync + SSE streaming
- CONS-12: SSE streaming execution support implemented
- Loading state properly managed
- Error handling in place
</success_criteria>

<output>
After completion, create `.planning/phases/05-console-feature/05-02-SUMMARY.md`
</output>
