---
phase: 05-console-feature
plan: 05
type: execute
wave: 4
depends_on: ["05-01", "05-02", "05-03", "05-04"]
files_modified:
  - frontend/src/components/console/status-indicator.tsx
  - frontend/src/components/console/index.ts
  - frontend/src/app/(console)/console/page.tsx
  - frontend/tests/components/console/status-indicator.test.tsx
  - frontend/tests/integration/console-page.test.tsx
autonomous: true
requirements:
  - CONS-13
user_setup: []
must_haves:
  truths:
    - "User can see real-time status indicator during execution"
    - "Status indicator shows: idle, connecting, receiving, complete, error"
    - "Connecting and receiving states show animated spinner"
    - "Console page composes all components into cohesive layout"
    - "Split panel layout shows input controls on left, results on right"
  artifacts:
    - path: "frontend/src/components/console/status-indicator.tsx"
      provides: "Flow state indicator with animation"
      exports: ["StatusIndicator"]
    - path: "frontend/src/app/(console)/console/page.tsx"
      provides: "Complete console page composition"
  key_links:
    - from: "frontend/src/components/console/status-indicator.tsx"
      to: "frontend/src/lib/stores/slices/console.ts"
      via: "connectionState"
      pattern: "state\\.connectionState"
    - from: "frontend/src/app/(console)/console/page.tsx"
      to: "frontend/src/components/console/*"
      via: "Component imports"
      pattern: "from '@/components/console'"
    - from: "frontend/src/app/(console)/console/page.tsx"
      to: "frontend/src/components/layout/split-panel.tsx"
      via: "SplitPanel"
      pattern: "SplitPanel"
---

<objective>
Build status indicator component and compose complete console page with split panel layout.

Purpose: Provide real-time feedback and complete the console feature by composing all components.
Output: StatusIndicator component, Console page
</objective>

<execution_context>
@/home/guyi/.claude/get-shit-done/workflows/execute-plan.md
@/home/guyi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-console-feature/05-RESEARCH.md
@.planning/phases/05-console-feature/05-VALIDATION.md

## ConnectionState Type (from 05-01)

```typescript
type ConnectionState = 'idle' | 'connecting' | 'receiving' | 'complete' | 'error'
```

## Existing Layout Component

From frontend/src/components/layout/split-panel.tsx:
```typescript
interface SplitPanelProps {
  left: React.ReactNode
  right: React.ReactNode
  defaultLayout?: number[]
  direction?: 'horizontal' | 'vertical'
  className?: string
}
```

## Console Route Group

From frontend/src/app/(console)/layout.tsx:
- Console layout exists with sidebar
- Route group provides layout separation

## Icons Available

From lucide-react:
- Loader2 (spinner)
- CheckCircle2 (success)
- XCircle (error)
- Radio (idle/receiving)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create StatusIndicator component</name>
  <files>
    frontend/src/components/console/status-indicator.tsx
    frontend/tests/components/console/status-indicator.test.tsx
  </files>
  <behavior>
    - StatusIndicator renders badge with icon and label
    - idle: Radio icon, muted styling, label "Idle"
    - connecting: Loader2 spinner, amber/warning styling, label "Connecting..."
    - receiving: Radio animated, primary styling, label "Receiving..."
    - complete: CheckCircle2, emerald/success styling, label "Complete"
    - error: XCircle, destructive styling, label "Error"
    - connecting and receiving states show animate-spin on icon
  </behavior>
  <action>
    Create the StatusIndicator component that visualizes connection state.

    1. Create `frontend/src/components/console/status-indicator.tsx`:
       - Props: state (ConnectionState), optional className
       - Use Loader2, CheckCircle2, XCircle, Radio from lucide-react
       - Define stateConfig object mapping states to icons, labels, and classNames
       - Render inline-flex container with icon and label
       - Use rounded-full badge styling
       - Apply animate-spin for connecting/receiving states
       - Use semantic colors: amber for connecting, emerald for complete, destructive for error

    2. Create `frontend/tests/components/console/status-indicator.test.tsx`:
       - Test: renders each state correctly
       - Test: shows correct icon for each state
       - Test: shows correct label for each state
       - Test: animate-spin applied to connecting state
       - Test: animate-spin applied to receiving state

    Reference the RESEARCH.md Pattern 4 for the stateConfig pattern.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run tests/components/console/status-indicator.test.tsx</automated>
  </verify>
  <done>StatusIndicator component displays real-time connection state</done>
</task>

<task type="auto">
  <name>Task 2: Create Console page composition</name>
  <files>frontend/src/app/(console)/console/page.tsx</files>
  <action>
    Create the complete console page that composes all console components.

    1. Create `frontend/src/app/(console)/console/page.tsx`:
       - 'use client' directive
       - Import SplitPanel from @/components/layout/split-panel
       - Import all console components from @/components/console
       - Use useStore to access result, connectionState, error
       - Render SplitPanel with defaultLayout=[40, 60]

       Left panel (40%):
       - Container with padding and space-y-4
       - EndpointSelector
       - MessageInput
       - ModelJsonPanel
       - ConfigPanel
       - Footer row: StatusIndicator + ExecuteButton

       Right panel (60%):
       - Container with padding and space-y-4
       - ErrorDisplay (if error) - will be created in 05-06
       - ResultDisplay (if result)
       - Timeline (if result.toolCalls)
       - ArtifactsList (if result.artifacts)
       - DebugOutput

       For now, inline the error display logic (error check and message).
       ErrorDisplay component will be extracted in 05-06.

    Follow the existing AgentConsole structure but use modular components.
  </action>
  <verify>
    <automated>test -f frontend/src/app/\(console\)/console/page.tsx</automated>
  </verify>
  <done>Console page composes all components into cohesive experience</done>
</task>

<task type="auto">
  <name>Task 3: Create console page integration test</name>
  <files>frontend/tests/integration/console-page.test.tsx</files>
  <action>
    Create an integration test for the complete console page.

    1. Create `frontend/tests/integration/console-page.test.tsx`:
       - Render ConsolePage wrapped in AppStoreProvider
       - Test: all input controls are present
       - Test: execute button triggers state change
       - Test: status indicator shows connection state
       - Test: result display shows after execution (mocked)
       - Test: split panel layout renders correctly

    Use MSW or mock fetch for API mocking in integration tests.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run tests/integration/console-page.test.tsx</automated>
  </verify>
  <done>Console page integration test verifies full page composition</done>
</task>

<task type="auto">
  <name>Task 4: Update console barrel export</name>
  <files>frontend/src/components/console/index.ts</files>
  <action>
    Update barrel export to include StatusIndicator.

    Update `frontend/src/components/console/index.ts`:
    ```typescript
    export { EndpointSelector } from './endpoint-selector'
    export { MessageInput } from './message-input'
    export { ModelJsonPanel } from './model-json-panel'
    export { ConfigPanel } from './config-panel'
    export { ExecuteButton } from './execute-button'
    export { ResultDisplay, StatusHeader, MetricsGrid } from './result-display'
    export { Timeline } from './timeline'
    export { ReportSummary } from './report-summary'
    export { ArtifactsList } from './artifacts-list'
    export { DebugOutput } from './debug-output'
    export { StatusIndicator } from './status-indicator'
    ```
  </action>
  <verify>
    <automated>grep -q "StatusIndicator" frontend/src/components/console/index.ts</automated>
  </verify>
  <done>Barrel export updated with StatusIndicator</done>
</task>

</tasks>

<verification>
- StatusIndicator component with tests
- Console page composition
- Console page integration test
- All tests pass: `cd frontend && npm test -- --run`
</verification>

<success_criteria>
- CONS-13: Flow state indicator shows real-time connection state
- Console page provides complete experience with split panel layout
- All components integrated and functional
</success_criteria>

<output>
After completion, create `.planning/phases/05-console-feature/05-05-SUMMARY.md`
</output>
